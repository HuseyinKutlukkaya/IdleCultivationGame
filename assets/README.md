# assets/

Static media for the game — images, icons, sprites, fonts, audio.

This folder is intentionally empty for now. Drop future art and audio
assets here and reference them with relative paths (e.g. `assets/sprites/qi.png`)
so the game stays hostable on GitHub Pages with no build step.
